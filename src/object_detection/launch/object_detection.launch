<launch>

  <!-- object_detection -->
  <node name="object_detection" pkg="object_detection" type="object_detection_node" output="screen">
    <rosparam command="load" file="$(find object_detection)/config/object_detection_config.yaml" />
  </node>

</launch>
